# Hoisted node_modules layout (npm/yarn-style flat tree).
# React Native / Metro expect certain packages to be hoisted; pnpm's default
# strict symlinked layout can break Expo. Hoisted is the reliable choice for
# an Expo-in-pnpm-monorepo. Applies to the whole workspace (fine for the api too).
node-linker=hoisted
